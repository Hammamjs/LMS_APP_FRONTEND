import { InstructorsListComponent } from '@/features/instructors/components';

export const metadata = {
  title: 'Instructors — LearnHub',
  description: 'Browse our talented instructors and their courses.',
};

export default function InstructorsPage() {
  return <InstructorsListComponent />;
}
