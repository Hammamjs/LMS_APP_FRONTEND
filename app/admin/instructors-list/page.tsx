import { AdminInstructorsComponent } from '@/features/auth/components';

export const metadata = {
  title: 'Admin · Instructors — LearnHub',
  description: 'Manage instructor accounts, status and permissions.',
};

export default function AdminInstructorsPage() {
  return <AdminInstructorsComponent />;
}
